function upperCase(word)
    {
    if(word[0] === word[0].toUpperCase() 
        && word[0].isNaN)
            {
                return "Word begins with an upper case letter";
            }else{ 
                return "Word does not begin with an upper case letter";    
            };
    };
    upperCase("T");